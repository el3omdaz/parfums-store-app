# إعداد المتجر من المتصفح فقط

لا تحتاج إلى Terminal أو أوامر برمجية.

## 1) رفع الملفات إلى GitHub

1. افتح مستودع `parfums-store-app` في GitHub.
2. احذف ملفات النسخة القديمة أو أنشئ فرعًا احتياطيًا قبل الاستبدال.
3. اضغط **Add file** ثم **Upload files**.
4. ارفع جميع محتويات هذا المجلد، وليس ملف ZIP نفسه.
5. تأكد أن `package.json` و`server.js` ظاهران في الصفحة الرئيسية للمستودع.
6. اضغط **Commit changes**.

## 2) تجهيز Supabase

1. افتح مشروع Supabase.
2. افتح **SQL Editor**.
3. افتح ملف `db/schema.sql` من GitHub وانسخ محتواه كاملًا.
4. الصقه في SQL Editor واضغط **Run**.
5. الملف مصمم كتحديث آمن: ينشئ الجداول والأعمدة الناقصة ولا يحذف المنتجات والطلبات الموجودة.
6. من إعدادات مشروع Supabase افتح صفحة مفاتيح API وانسخ:
   - Project URL.
   - Secret key الجديد، أو `service_role` في المشاريع القديمة.
7. لا تضع المفتاح السري في GitHub أو داخل ملفات الواجهة.

## 3) ربط GitHub مع Railway

1. افتح Railway وأنشئ **New Project**.
2. اختر النشر من GitHub أو أنشئ Service ثم اختر **Connect Repo**.
3. اختر مستودع `parfums-store-app`.
4. Railway سيتعرف على `package.json` ويشغل `npm start` تلقائيًا.
5. افتح خدمة التطبيق ثم تبويب **Variables**.
6. أضف المتغيرات التالية واحدًا واحدًا:

| الاسم | القيمة |
|---|---|
| `SUPABASE_URL` | Project URL من Supabase |
| `SUPABASE_SECRET_KEY` | Secret key من Supabase |
| `JWT_SECRET` | نص عشوائي طويل من مدير كلمات المرور |
| `ADMIN_JWT_SECRET` | نص عشوائي طويل آخر ومختلف |
| `ADMIN_PASSWORD` | كلمة مرور قوية للوحة الإدارة |

7. راجع التغييرات واضغط **Deploy** لتطبيق المتغيرات.
8. افتح **Settings** ثم **Networking / Public Networking** وأنشئ Domain إذا لم يظهر رابط عام تلقائيًا.

## 4) اختبار التطبيق

1. افتح رابط Railway: يجب أن يظهر المتجر.
2. افتح `/health`: يجب أن ترى `status: ok` و`supabase_configured: true`.
3. افتح `/admin` وسجل الدخول بكلمة `ADMIN_PASSWORD`.
4. أضف منتجًا تجريبيًا مع كمية في المخزون.
5. افتح المتجر في نافذة خاصة وأرسل طلبًا تجريبيًا.
6. ارجع إلى لوحة الإدارة وتأكد أن الطلب ظهر وأن المخزون انخفض.

## 5) الإعدادات التي تغيّرها من لوحة الإدارة

من قسم **إعدادات المتجر** تستطيع تغيير:

- اسم المتجر ووصفه.
- رمز العملة ومفتاح الدولة.
- عدد أرقام الهاتف المحلي.
- السعر الموحد.
- رسوم التوصيل العادي والبعيد.
- حد الشحن المجاني.
- رقم واتساب.
- طرق الدفع الظاهرة.

لا تحتاج إلى تعديل الملفات عند تغيير الدولة أو العملة.

## 6) Twilio اختياري

المتجر يسمح بالطلب كزائر، لذلك لا تحتاج Twilio لبدء البيع. عند رغبتك بتسجيل العملاء برمز تحقق، أضف متغيرات Twilio في Railway من تبويب Variables. لا تضع مفاتيح Twilio في GitHub.

## ملاحظات أمان

- استخدم `SUPABASE_SECRET_KEY` فقط داخل Railway Variables.
- لا تستخدم Publishable/Anon key في الباك إند بدل المفتاح السري.
- اجعل `JWT_SECRET` و`ADMIN_JWT_SECRET` مختلفين وطويلين.
- لا تفعّل «الدفع الإلكتروني» من الإعدادات قبل ربط بوابة دفع فعلية.
