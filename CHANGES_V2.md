# أهم تعديلات النسخة الثانية

- توحيد الواجهة والباك إند في خدمة Railway واحدة.
- إزالة رابط Railway الثابت من الواجهة واستخدام نفس النطاق تلقائيًا.
- إضافة قسم نيش وتصنيفات نسائي ورجالي ويونيسكس.
- إكمال السلة والكميات ونموذج العنوان وإنشاء الطلب.
- احتساب الأسعار والتحقق من المخزون داخل قاعدة البيانات.
- إعادة المخزون تلقائيًا عند إلغاء الطلب.
- تشغيل طلب العطر الخاص وحفظه في Supabase.
- إضافة البحث والمفضلة وتتبع الطلبات وتسجيل الدخول الاختياري.
- تأمين لوحة الإدارة باستخدام JWT وعدم إرسال كلمة مرور الإدارة للواجهة كرمز.
- إضافة إعداد العملة والدولة ورسوم التوصيل من لوحة الإدارة.
- إضافة إدارة الصور والماركات والمخزون والمحذوفات والنسخ الاحتياطية.
- تحديث مخطط Supabase بطريقة تحافظ على البيانات الموجودة.

## تحديث إضافي — نسخة أدق بصريًا
- تقريب الرئيسية وقسم النيش بشكل أوضح من الصور المرجعية.
- إضافة صور SVG تجريبية داخل `assets/demo/` للبانرات والمنتجات.
- أي منتج لا يملك `image_url` من الباك إند سيعرض صورة تجريبية تلقائيًا.
- يمكن استبدال الصور من الباك إند عبر قيمة `image_url`، أو حذفها ليظهر الـ fallback التجريبي.
